apiVersion: apps/v1 
kind: Deployment 
metadata: 
  name: sba2022.kubernetes-cluster-deployment
spec:
  selector:
    matchLabels:
      app: sba2022.kubernetes-cluster
  replicas: 3 
  template:
    metadata:
      labels:
        app: sba2022.kubernetes-cluster
    spec:
      containers:
      - name: sba2022.kubernetes-cluster-container 
        image: buildapp/webapp
        ports:
        - containerPort: 5000
---

apiVersion: v1
kind: Service
metadata:
  name: sba2022.kubernetes-cluster-service
spec:
  type: NodePort
  selector:
    app: sba2022.kubernetes-cluster
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 32000
